class Solution(object):
    def findDisappearedNumbers(self, nums):
        result=[]
        for i in range(1,len(nums)+1):
            if not i in nums:
                result.append(i)
        return result
